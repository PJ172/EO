# 📋 Changelog — 26/03/2026

## Module: Phòng Họp (Meeting Room)

### Phase 6: UI/UX Overhaul

#### 1. Sidebar Calendar Fix
- **File:** `apps/web/src/components/bookings/booking-sidebar.tsx`
- Fix mũi tên `< >` bị chồng lên "Tháng Ba 2026" → chuyển sang `flexbox` + `ChevronLeft/ChevronRight`
- Fix cột T7/CN bị cắt bên phải

#### 2. Room Dialog Redesign (lần 1)
- **File:** `apps/web/src/components/bookings/room-dialog.tsx`
- Layout 2 cột (3:2) với sections accent bars (blue/emerald/amber)
- Live Preview card + drag-and-drop image upload

#### 3. Calendar View & Toolbar
- **File:** `apps/web/src/components/bookings/calendar-view.tsx`
- Toolbar dạng pill navigation + segmented view selector
- Empty states cho Gantt/Calendar khi chưa có phòng

#### 4. Booking Form Polish
- **File:** `apps/web/src/components/bookings/booking-form.tsx`
- Fix Purple Ban (thay indigo/violet → cyan/blue)
- Room selector với color swatches

---

### Phase 7: UI Breakthrough Redesign

#### 1. Room Dialog — Compact Single-Column
- **File:** `apps/web/src/components/bookings/room-dialog.tsx`
- **Trước:** 2-column `grid-cols-5` → khoảng trống lớn phía dưới
- **Sau:** Single-column `max-w-[540px]`, `flex flex-col` overflow, sticky footer
- Name + Capacity inline, color picker 1 hàng ngang, image upload compact `h-20`
- Equipment/Features tags compact với accent bars

#### 2. Sidebar Calendar — Professional Container
- **File:** `apps/web/src/components/bookings/booking-sidebar.tsx`
- **Trước:** Arrows tách khỏi title, T7/CN cắt, không có container
- **Sau:** Container `rounded-xl border bg-white shadow-sm p-3`
- DayPicker v9 classNames: `month_caption` relative, `nav` absolute flanking title
- Cells `34x34px`, hover `blue-50`, selected `blue-600`, today `font-bold blue-600`

#### 3. Calendar CSS — Ultra-Clean
- **File:** `apps/web/src/components/bookings/calendar-custom.css`
- Grid borders: `#f8fafc` (slate-50) thay vì `#e2e8f0` (quá đậm)
- Today highlight: `box-shadow: inset 0 -2px 0 0 #3b82f6` (bottom-border accent) thay vì heavy ring
- Header row: `font-weight: 500`, `color: #94a3b8`, `font-size: 0.7rem`
- Events: `box-shadow: 0 1px 2px rgba(0,0,0,0.06)` micro-shadow
- Off-range days: `opacity: 0.25` → mờ hơn

#### 4. Calendar Toolbar — Thinner
- **File:** `apps/web/src/components/bookings/calendar-view.tsx`
- Tất cả elements `h-7` (từ `h-8`), icons `3.5` (từ `4`)
- Removed duplicate `+ Đặt phòng` button (đã có trong page header)
- Border: `border-slate-100` thay vì default
- Title: `text-base font-semibold` (từ `text-lg font-bold`)

#### 5. Booking Form — Full Data Display
- **File:** `apps/web/src/components/bookings/booking-form.tsx`
- Layout: `max-w-5xl` → `max-w-6xl` (rộng hơn, bớt trống 2 bên)
- Right column `lg:sticky lg:top-4` chứa:
  - **Room Quick Info Card** — image, name, description, capacity, ALL equipment tags, đặc điểm (amber tags)
  - **Settings Card** — Công khai/Riêng tư, Gửi email (compact `p-4`)
  - **Meeting Summary Card** — live preview: title, date/time, attendees, visibility tags
- Form compact: `p-6→p-4`, `space-y-6→space-y-4`, textarea `min-h-120→80px`

---

### Files đã sửa (tổng cộng 5 files)

| File | Loại thay đổi |
|------|--------------|
| `apps/web/src/components/bookings/room-dialog.tsx` | Rewrite hoàn toàn |
| `apps/web/src/components/bookings/booking-sidebar.tsx` | Calendar classNames + container |
| `apps/web/src/components/bookings/calendar-custom.css` | Rewrite CSS |
| `apps/web/src/components/bookings/calendar-view.tsx` | Toolbar compact |
| `apps/web/src/components/bookings/booking-form.tsx` | Layout + Room Info + Summary |

### Design System

- **Palette:** Slate + Blue + Emerald + Cyan + Amber (NO Purple)
- **Typography:** Plus Jakarta Sans
- **Geometry:** `rounded-lg` (8px), sharp professional edges
- **Shadows:** Micro-shadows `0 1px 2px rgba(0,0,0,0.06)`
