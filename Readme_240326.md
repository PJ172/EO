# CẬP NHẬT DỰ ÁN (24/03/2026)

## 1. Hệ thống Phân cấp Tổ chức (L1-L7)
- **Database Schema**: Thêm trường `orgLevel` (String) vào bảng `Employee` trong `schema.prisma`.
- **Backend Service (`employee-query.service.ts`)**: 
  - Cập nhật hàm `getDeptOrgChart` tiêm vào (inject) chuỗi dữ liệu Global Context để luôn báo cáo xuyên tâm lên Chủ tịch/Tổng giám đốc. Các nút này được đánh dấu là `isGlobalContext: true`.
- **Frontend Employee Form (`employee-form.tsx`)**:
  - Giao diện Admin/HR cho phép chọn gán thủ công phân cấp từ L1 đến L7 cho từng nhân viên, ghi đè logic dò tìm theo chức danh.
- **Sơ đồ UI (Org Chart)**:
  - Hiển thị UI Badge có màu cho từng Level (L1 đỏ sậm, L7 xanh lam). 
  - Đánh dấu Node Global bằng viền gạch ngang (dashed) và độ mờ để phân biệt với nhân sự trực thuộc phòng ban.

## 2. Quản trị & Cơ sở Dữ liệu
- Cập nhật Prisma Schema (`npx prisma db push` và `generate`).
- Chạy `seed.ts` thành công: Khởi tạo dữ liệu roles (ADMIN, HR, MANAGER, EMPLOYEE), permissions và các mock categories.
- Chạy `create-admin-user.ts`: Tạo ra 2 user quản trị (`admin / 123456` và `it / 123456`) với toàn quyền thao tác.

## 3. Các sửa lỗi & Cải tiến Frontend
- **Lỗi Duplicate Toast**: Sửa lỗi 1 thao tác bấm lưu sinh ra 2 thông báo trên màn hình do bị gọi trùng `toast` ở cả tầng Hook `employee.service.ts` và tầng View UI `employment-events-tab.tsx`.
- **Lỗi Lint TypeScript**: Khắc phục lỗi Implicit Any đối với tham số `emp` trong `employee-form.tsx` gây ra lỗi build ở thẻ Combobox "Người quản lý".

---

## 4. Tích hợp Position (Vị trí / Chức vụ) vào Form Nhân viên

- **Frontend (employee-form.tsx)**: Thêm trường Combobox **"Vị trí / Chức vụ"** (positionId) vào nhóm Thông tin Đơn vị. Import hook usePositions, fetch danh sách, thêm vào ieldsToClean.
- **Validation (schemas.ts)**: Thêm positionId: z.string().nullable().optional() vào employeeSchema.
- **Đổi nhãn**: jobTitleId đổi nhãn từ "Chức vụ" → **"Chức danh"** để phân biệt rõ với Position.

## 5. Auto-Capitalize Dữ liệu Nhân viên

- **Backend (employee.service.ts)**: Thêm helper capWords() vào cả create() và update(), áp dụng cho 6 trường: permanentAddress, 	emporaryAddress, irthPlace, ethnicity, eligion, placeOfIssue.
- **Migration DB**: Chạy SQL INITCAP() trực tiếp trên PostgreSQL, cập nhật 367-368 bản ghi hiện có.

## 6. Xóa Cấp Bậc (L1-L7) khỏi Ứng dụng

- **employee-form.tsx**: Xóa Select dropdown "Cấp bậc (L1-L7)" và orgLevel khỏi default values.
- **employee-node.tsx**: Xóa Level Badge (L1/L2...) trên Org Chart node. Cột orgLevel giữ trong DB.

## 7. Tái cấu trúc Form Nhân viên

- **Xóa khỏi Tab Cơ bản**: "Người giới thiệu" (Thông tin Đơn vị), "Ghi chú" (Thông tin Địa chỉ).
- **Xóa khỏi Tab Hồ sơ**: "Link File Hồ sơ (Drive/Server)" (documentFile).
- **Thêm vào Tab Hồ sơ → Thông tin khác**: "Người giới thiệu" + "Ghi chú" (Textarea).

## 8. Sửa Lỗi Org Chart — Nodes & Arrow Connections

Phân tích 3 file (~1600 dòng) và sửa 5 vấn đề:

| # | Vấn đề | File | Fix |
|---|---|---|---|
| 1 | Chấm hồng trên employee nodes | employee-node.tsx | Handles: !bg-transparent !border-transparent trong lock mode |
| 2 | Chấm hồng trên structure nodes | structure-node.tsx | Cùng fix, handle thu nhỏ 2.5×2.5 |
| 3 | Collapse button bị edge che | employee-node.tsx | z-index z-30 → z-50 + e.preventDefault() |
| 4 | Nodes chồng lấn (dagre sai height) | org-chart-canvas.tsx | NODE_H 210→220, V_GAP 120→140 |
| 5 | Đường ngang nối siblings | org-chart-canvas.tsx | Pin sourceHandle='bottom'→targetHandle='top'; emp→emp dùng ezier; stroke 2px, màu #64748b, arrow 14px |