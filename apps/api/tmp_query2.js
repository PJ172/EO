const { PrismaClient } = require('./node_modules/.prisma/client'); const p = new PrismaClient(); async function m() { const lan = await p.employee.findFirst({ where: { fullName: { contains: 'Trần Thị Phương Lan' } }, select: { id: true, managerEmployeeId: true } }); console.log('Lan manager:', lan.managerEmployeeId); const ha = await p.employee.findFirst({ where: { fullName: { contains: 'Nguyễn Phương Hà' } }, select: { id: true, managerEmployeeId: true, departmentId: true } }); console.log('Ha dept:', ha.departmentId); await p.$disconnect(); } m();