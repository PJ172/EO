import { CarForm } from "@/components/cars/car-form";

export const metadata = {
    title: "Thêm xe mới | eOffice",
    description: "Thêm thông tin xe mới vào hệ thống",
};

export default function NewCarPage() {
    return <CarForm />;
}
